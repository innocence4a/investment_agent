# CLAUDE.md — investment_agent 開発ルール

AI エージェントが自動売買する様子をリアルタイムダッシュボードで観察するシステム。
**仕様の正は `docs/requirements.md`(要件定義書)**。本ファイルと矛盾したら要件定義書が勝つ。
デザインの正は `docs/dashboard-mockup.html`(動くモック)。実装はこのトーン・配色・レイアウトに合わせる。

## 絶対に守る安全ルール(変更には発注者の明示的承認が必要)

1. **リスク制約はコードで強制する。** 1取引上限・日次損失上限・総エクスポージャ上限・取引頻度・損切りラインは、LLM の出力に関係なく発注前にコードで検査し、違反は発注拒否する。プロンプトだけで制約を表現してはならない
2. **権限の序列**: コードのリスクゲート > リスク管理エージェント > トレーダーエージェント。相談役エージェントは助言のみ(売買権限なし)。この序列を迂回する実装をしない
3. **緊急停止はエージェント・コア側の状態**として保持(未約定注文の取消+新規停止、損切り監視は継続)。ダッシュボードやブラウザの生死に依存させない
4. **PAPER / LIVE は同一インターフェース**で実装し、切替は設定 1 箇所。現在モードを画面に常時表示。LIVE 接続(Phase 2)は発注者の明示的な承認なしに実装・有効化しない
5. **全判断(見送り含む)・全取引・全 API 応答を記録**する。判断には日本語の根拠テキストを必ず付ける
6. **秘密情報(API キー・シークレット)をリポジトリに含めない。** 環境変数+シークレット管理で扱う。取引所キーは出金権限なしが前提
7. イベント(ニュース・要人発言)単独では発注しない。高インパクトイベントのデフォルト反応は防御(新規停止)

## 技術スタック(統一。変更提案は要件定義書の改訂として出すこと)

- **バックエンド**: Python 3.12+ / asyncio。型ヒント必須(mypy strict が通ること)、pydantic v2 でモデル定義、フォーマット・リントは ruff
- **ダッシュボード**: TypeScript(strict)+ Vite。チャートはモック同様 Canvas 自前描画(外部チャートライブラリ禁止— モックの見た目を正とするため)。CSS はモックのデザイントークン(`--bg` `--surface` `--up` `--down` 等)を移植して使う
- **DB**: SQLite(Phase 1。単一ユーザー・監査ログ用途に十分)。スキーマ変更はマイグレーションスクリプトを伴うこと
- **LLM**: Anthropic API。エージェント役割ごとにモデルを分ける(トレーダー=通常モデル、リスク管理=軽量モデル+ルール併用、相談役=日次のみ)。API 呼び出しは必ずタイムアウト・リトライ・月次コスト上限カウンタを通す
- **通信**: コア→ダッシュボードは WebSocket push(画面反映 1 秒以内が要件 N-1)

## 金額・時刻・数値の扱い

- **金額に float を使わない。** JPY は int(円)、暗号資産数量は Decimal で保持。表示時のみフォーマット
- 保存・内部処理は **UTC**、表示は **JST**。naive datetime 禁止
- 画面の数値は等幅(tabular-nums)で桁を揃える。上昇=グリーン/下落=レッドを一貫させ、色だけに意味を持たせない(▲▼・+/− を併記)

## 品質ゲート(すべて満たすまでマージ・完了報告をしない)

1. **リスクゲートの全制約にユニットテスト**(上限ちょうど・超過・境界値)。キルスイッチ発動時の挙動テスト必須
2. ペーパーブローカーの約定・残高・損益計算にテスト(端数・スリッページ含む)
3. `ruff check` / `mypy` / `tsc --noEmit` / 全テストがゼロエラーで通ること
4. **ダッシュボードの変更は実ブラウザで検証**する。Playwright で `deviceScaleFactor: 2`(高解像度)を必ず含めること — 過去に DPR=1 のみの確認で「チャートが無限に伸びる」バグを見逃した実績がある。数十秒稼働させてレイアウト・サイズが安定していることも確認する
5. LLM 呼び出し部分は API をモックしてテスト(実 API に依存するテストを CI に入れない)
6. 動作確認せずに「動きます」と報告しない。確認した内容(コマンド・結果)を報告に含める

## 作法

- **言語**: ドキュメント・コミットメッセージ・画面文言・判断根拠は日本語。コードの識別子は英語、コメントは日本語可
- **コミット**: 1 コミット 1 まとまり。メッセージは日本語で「何を・なぜ」
- **仕様変更を伴う実装をしたら `docs/requirements.md` を同じ PR/コミットで改訂**し、冒頭のバージョン(v0.x)を上げて変更点を追記する。コードと要件定義書を乖離させない
- `docs/dashboard-mockup.html` はデザインリファレンスとして保持する(実装で置き換えても削除しない)。モックを変更したら上記の Playwright 検証を行う
- 判断に迷う仕様は勝手に決めず、要件定義書 §11(決定事項・未決事項)に選択肢と推奨を追記して発注者に確認する

## ディレクトリ構成(実装開始時にこの形で作る)

```
core/        # エージェント・コア(Python): フィード, エージェント3役, リスクゲート, ペーパーブローカー, 記録
dashboard/   # ダッシュボード(TypeScript + Vite)
docs/        # requirements.md(正), dashboard-mockup.html(デザイン正), ADR 等
tests/       # core のテスト
```

## 現在の状態(2026-07-12 時点)

- Phase 0(要件定義・デザインモック)完了。対象市場は**仮想通貨(BTC/ETH)で確定**
- 次は Phase 1: 実データ(取引所公開 WebSocket)+ Claude 実判断のペーパートレード実装(F-1〜F-4, F-6〜F-13)
- Phase 1.5 以降の機能(イベント連携・マルチエージェント・カレンダー等)は要件定義書 §4・§7・§12 を参照
