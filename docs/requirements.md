# 投資エージェント 要件定義書 v0.7

- 作成日: 2026-07-10(v0.6: **Phase 1 実装(F-1〜F-4, F-6〜F-13)に伴う実装決定事項を §11 に追記**。データ源=bitFlyer 公開 WebSocket、ペーパーは現物ロングのみ、日次集計は JST 暦日、開発検証用の sim フィード・mock 判断モードを追加 / v0.7: **Phase 1.5 ①実装(F-18〜F-21)に伴う決定事項を §11 に追記**。関連指標=alternative.me+Yahoo Finance 無料ソース、カレンダー=静的ファイル+NFP ルール生成、リスク管理エージェント=ルールベースで抑制モードをリスクゲート層に強制、相談役=日次レビューを思考ログ+Slack に出力)
- ステータス: ドラフト(発注者確認待ち)
- 参照元: https://x.com/robrtcode/status/2064361958392938550(ビジュアルの参考。Polymarket・戦略の複製はスコープ外)

---

## 1. プロダクトの定義

**「AI エージェントが自動売買している様子を、リアルタイムのダッシュボードで眺められるシステム」**

参照ポストから引き継ぐのは「見て面白いクオンツ風ダッシュボード」の体験であり、Polymarket や高頻度アービトラージ戦略そのものは複製しない。価値の中心は次の 2 つ:

1. **観察体験**: エージェントの判断(なぜ買った/売った/見送ったか)と成績がリアルタイムに流れるダッシュボード。デザインは参照画像より「見やすく・オシャレ」に(§6)
2. **実際に動く自動売買**: まずはペーパートレード(仮想売買)で本物の市場データを使って動かし、検証後に少額実弾へ

## 2. 対象市場: 仮想通貨を推奨(株式は代替案)

| 観点 | 仮想通貨(推奨) | 株式(国内) |
|------|----------------|--------------|
| 市場時間 | **24時間365日 → いつ見ても動いている** | 平日 9:00〜15:30 のみ。夜・週末は静止画になる |
| リアルタイムデータ | 取引所の公開 WebSocket で**無料・口座不要・即日入手** | リアルタイム板は証券口座+API 審査が必要 |
| ペーパートレード開始 | **口座なしで今日から可能** | データ取得の時点で口座開設が必要 |
| 実弾移行 | 国内交換業者 API(bitFlyer / GMOコイン等)で少額から | kabuステーション API 等(審査あり) |
| 判断サイクルとの相性 | ボラティリティが高く、分単位の判断でも画面が動く | 日次サイクル向き。「眺める」体験は単調になりがち |

**決定(2026-07-11)**: Phase 1〜2 は仮想通貨(BTC/ETH 中心)で確定。FX(USD/JPY、OANDA デモ口座)を次のアセットとして F-14 に位置づけ、株式はその後。

## 3. システム構成

```
[取引所 公開 WebSocket]──リアルタイム価格──┐
[イベントフィード]──要人発言・経済指標・ニュース─┤
                                            ▼
┌─ エージェント・コア(常駐プロセス)───────────────────┐
│ ① マーケット状態の集約(価格・出来高・テクニカル指標)      │
│ ② LLM 判断サイクル(1〜5分毎): Claude が売買/見送りを判断し │
│    「判断根拠」を日本語で出力 ← ダッシュボードの主役コンテンツ │
│ ③ リスクゲート(コード強制): 上限・損失・頻度チェック        │
│ ④ 執行: Phase 1 = ペーパーブローカー(仮想約定)             │
│         Phase 2 = 取引所 API(少額実弾)                     │
│ ⑤ 記録: 全判断・全取引・損益を DB へ                        │
└──────────────┬───────────────────┘
                              ▼ WebSocket push(1秒以内)
┌─ ダッシュボード(Web アプリ)─────────────────────┐
│ KPI ヘッダ / 価格チャート+売買マーカー / 資産曲線            │
│ エージェント思考ログ / ポジション / 取引フィード / 緊急停止    │
└──────────────────────────────────┘
```

**設計上の決定事項**
- Polymarket 型(31回/時)と違い高頻度ではないため、**LLM を判断ループに入れる**(1〜5 分サイクル)。「Claude がいま何を考えているか」が画面に流れることが本プロダクトの見どころであり、これは意図した設計
- ただしリスク制約(1取引上限・日次損失上限・取引頻度上限)は従来どおり**コードで強制**し、LLM の出力より優先する
- ペーパーブローカーと実弾ブローカーは同一インターフェースで実装し、切替は設定 1 箇所(ダッシュボードに PAPER / LIVE バッジを常時表示)

### マルチエージェント構成(役割分担・v0.5)

判断の質と安全性のため、単一エージェントではなく **3 つの役割**に分ける。思考ログにはどのエージェントの発言かを明示する。

| エージェント | 役割 | 実行頻度 | 権限 |
|--------------|------|----------|------|
| **トレーダー(メイン)** | 市場データ・インジケーター・関連指標をもとに売買判断と根拠を出力 | 判断サイクル毎(1〜5分) | 売買の**提案**(発注はリスクゲート通過後) |
| **リスク管理** | 経済指標カレンダー(F-19)・VIX 等・日次損益・連敗状況を監視し、「取引抑制モード」の発動/解除、ポジションサイズの制限を指示 | イベント/カレンダー駆動+定期 | トレーダーの提案への**拒否権**。抑制モード発動権 |
| **相談役** | マクロ環境・成績・判断ログを俯瞰してレビュー。地合いの解釈、戦略パラメータの改善提案、トレーダーが迷うケースのセカンドオピニオン | 日次+要求時(低頻度) | **助言のみ**(直接の売買権限なし。提案の適用は人間承認) |

- **権限の序列**: コードのリスクゲート(F-3)>リスク管理エージェント>トレーダー。相談役は序列外(助言のみ)。LLM 同士の合議で暴走しない構造にする
- **コスト設計**: トレーダーは通常モデル、リスク管理は軽量モデル+ルールベースの組合せ、相談役は日次のみ——役割ごとにモデルと頻度を変えて LLM コストを抑える(N-3)

## 4. 機能要件

### エージェント側
| # | 機能 | 内容 | 優先度 |
|---|------|------|--------|
| F-1 | 価格フィード | BTC/ETH 等の価格・出来高を公開 WebSocket で取得 | 必須 |
| F-2 | LLM 判断サイクル | 市場状態+ポジション+直近成績を Claude に渡し、売買/見送りと**日本語の判断根拠**を取得 | 必須 |
| F-3 | リスクゲート | 1取引上限・日次損失上限・総エクスポージャ上限・取引頻度をコードで強制。違反時は発注拒否+画面表示。保有ポジションの損切り/利確ラインは LLM の判断サイクルとは独立にコード側で常時監視 | 必須 |
| F-4 | ペーパーブローカー | 実勢価格+想定スリッページで仮想約定。残高・ポジション・損益を管理 | 必須 |
| F-5 | 実弾執行 | 国内取引所 API 経由の発注・約定管理(Phase 2) | Phase 2 |
| F-6 | 記録 | 全判断(見送り含む)・全取引・損益推移を DB 保存 | 必須 |
| F-7 | 通知 | 取引・エラー・ガードレール発動に加え、プロセス死活・価格フィード断を即時通知(Slack/LINE)。無人 24/365 稼働(N-2)の前提条件 | 必須 |

### ダッシュボード側
| # | 機能 | 内容 | 優先度 |
|---|------|------|--------|
| F-8 | KPI ヘッダ | 総資産・累計損益・勝率・取引数・稼働時間を一目で | 必須 |
| F-9 | 価格チャート | ローソク足+エージェントの売買マーカー(どこで買い/売りしたか)。タイムフレーム切替: 5分/1時間/4時間/日足/週足/月足。X軸は時刻ラベル+基準線 24 本(表示幅 24 本足: 月足なら 2 年分)。インジケーター: 移動平均線 3 本(MA7/25/99)をチャートに重畳、RSI(14)・MACD(12,26,9) をサブパネル表示(将来: ボリンジャーバンド・出来高等を追加可能な構造) | 必須 |
| F-10 | 資産曲線 | 資産推移(エクイティカーブ)のリアルタイム描画 | 必須 |
| F-11 | 思考ログ | エージェントの判断根拠を時系列カードで表示(**主役機能**) | 必須 |
| F-12 | ポジション/フィード | 保有ポジション一覧と約定フィードのライブ表示 | 必須 |
| F-13 | 緊急停止 | 停止状態はエージェント・コア側で保持: 未約定注文の取消+新規エントリー停止、損切り監視は継続、ダッシュボード切断後も停止状態を維持。画面のキルスイッチと通知からの操作はその入口。PAPER/LIVE バッジ常時表示 | 必須 |
| F-14 | マルチアセット | 監視銘柄の追加。第一候補は FX(USD/JPY 等): OANDA Japan API がデモ口座(無料)でストリーミングデータ+仮想売買を提供し、ペーパー/実弾を同一構成で追加可能。イベントドリブン判断(F-16)との相性が最も良い市場(要人発言・指標・為替介入で動く)。留意: 土日は市場休場(週明けの窓開きリスク)、レバレッジは国内最大 25 倍だがリスクゲートで実効 1〜3 倍に制限。株式タブはその後 | 将来 |
| F-15 | イベント収集 | 要人発言(米大統領・FRB 議長等)・経済指標・重要ニュースのヘッドラインをフィード経由で受信し、インパクト(高/中/低)を分類 | Phase 1.5 |
| F-16 | イベントドリブン判断 | 高インパクトのイベント検知時、定期サイクルを待たずに LLM 判断を即時起動。**デフォルト動作は防御(新規エントリー一時停止・ポジション縮小の検討)**。イベント単独では発注せず、価格反応と併せて判断 | Phase 1.5 |
| F-17 | イベント表示 | 検知イベントをダッシュボードにライブ表示し、それに対するエージェントの反応(思考ログ)と紐づけて見せる | Phase 1.5 |
| F-18 | 関連指標(クロスアセット) | 恐怖指数(VIX)・Crypto Fear & Greed・ゴールド・S&P500・ドル指数(DXY)・米10年金利をダッシュボードに常時表示し、**LLM 判断のインプットにも渡す**(例: ゴールド売られ+株先物堅調 → リスクオン資金フローと解釈)。データ源: Fear & Greed は alternative.me(無料)、VIX/ゴールド等は無料ソース(15〜20分遅延)で開始し、必要になれば有料リアルタイムに格上げ | Phase 1.5 |
| F-19 | 経済指標カレンダー | 雇用統計(NFP)・CPI・FOMC・日銀会合等の**発表スケジュールを事前管理**し、ダッシュボードに「今後の予定+カウントダウン」を表示。発表前後の**取引抑制ウィンドウ**(例: 前後 30 分は新規エントリー禁止・サイズ制限)をリスク管理エージェント経由で自動発動。仮想通貨にも適用(BTC は米指標に反応する)、FX 追加時(F-14)はそのまま流用 | Phase 1.5 |
| F-20 | リスク管理エージェント | §3 の役割分担参照。カレンダー・関連指標・損益状況から取引抑制モードを発動/解除し、根拠を思考ログに出力 | Phase 1.5 |
| F-21 | 相談役エージェント | §3 の役割分担参照。日次レビュー・地合い解釈・改善提案を思考ログとレポートに出力(適用は人間承認) | Phase 1.5 |
| F-22 | バックテスト | 収集済みの価格・イベント履歴に対してロジック変更を事前検証し、成績(勝率・PF・最大DD)を比較表示。※Phase 1 の稼働自体が検証用データ収集を兼ねる | Phase 2〜 |

## 5. 非機能要件

| # | 項目 | 要件 |
|---|------|------|
| N-1 | 更新レイテンシ | 価格・約定・思考ログの画面反映 1 秒以内(WebSocket push) |
| N-2 | 稼働 | 24/365 常駐。プロセス監視・自動再起動。障害時は新規発注停止に倒す |
| N-3 | コスト | LLM 呼び出しは判断サイクル毎のため月額が読める(例: 5分毎 ≒ 月 8,640 回)。月次上限とアラートを設定 |
| N-4 | セキュリティ | Anthropic・取引所の API キーはいずれもシークレット管理で保管し、コード・リポジトリに含めない。取引所キーは出金権限なし。ダッシュボードは認証必須(本人のみ) |
| N-5 | 監査性 | 全判断・全取引が時系列で追跡可能 |

## 6. デザイン要件(ダッシュボード)

参照画像の「クオンツ端末」の情報密度・ワクワク感は残しつつ、次の方針で「見やすく・オシャレ」に:

- **ダークテーマの取引端末**をベースに、参照画像のような CRT 風の過密表示は避け、余白と情報階層を明確に
- 数値はタブラー(等幅)数字で桁を揃え、上昇=グリーン/下落=レッドの意味色は常に一貫
- 主役は「エージェントの思考ログ」: 判断根拠が日本語で流れ、売買はチャート上のマーカーと連動
- KPI → チャート → 詳細(ポジション・フィード)の視線順に配置。装飾よりも一目で状態が分かること
- ※デザインモックアップ(動くプロトタイプ)を `docs/dashboard-mockup.html` として添付。これを叩き台にトーンを確定する

## 7. イベント連携(要人発言・経済指標)の設計方針

### 狙うこと・狙わないこと
- **狙わない**: ヘッドライン発表から数ミリ秒〜数百ミリ秒の速度勝負(プロの HFT ボットの土俵であり、LLM 判断の所要時間では構造的に勝てない)
- **狙う**: ①**防御** — 高インパクトイベント検知時に数秒〜数十秒で新規エントリー停止・縮小(損失回避は速度勝負に勝たなくても価値がある)、②**数分〜数時間の方向性判断** — 発言内容の解釈を Claude が行い、その後の判断サイクルのバイアスに反映

### データソース候補と概算コスト
| ソース | 内容 | レイテンシ | 月額目安 |
|--------|------|-----------|---------|
| 経済指標カレンダー(無料 API/RSS) | CPI・FOMC・雇用統計等の**予定**と結果 | 発表後数秒〜 | ¥0 |
| 暗号資産系ニュースアグリゲータ(Tree of Alpha / Phoenix News 等) | 通信社ヘッドライン+**要人の X ポストを既に集約**(ミリ秒〜秒) | 秒未満 | ¥0〜2万円程度 |
| X API(従量課金) | 特定アカウントの直接ポーリング | ポーリング間隔次第 | 読み取り $0.005/件。**リアルタイム stream は Enterprise(約 $42,000/月)限定のため対象外**。少数アカウントの補完用途のみ |
| 一般ニュース API(GNews 等) | 背景ニュース | 分単位 | ¥0〜数千円 |

**推奨**: アグリゲータ+経済指標カレンダーで開始(月 2 万円以下)。X API 直接契約は行わない(要人ポストはアグリゲータが秒未満で中継するため二重投資になる)。

### 安全設計(誤報対策)
- イベント単独では発注しない(F-16)。フェイクヘッドライン・なりすまし・誤報があるため、**価格の実反応と併せて**判断する
- ソースごとに信頼度を持ち、単一の低信頼ソースのみの場合は「警戒モード(新規停止)」までに制限
- 経済指標は「予定」が事前に分かるため、発表前後は自動的に新規エントリーを止める(カレンダー連動)

## 8. 必要な準備

1. Anthropic API キー(判断サイクル用。月額上限設定)
2. 実行環境: 常駐サーバ(VPS 等)+ ダッシュボードのホスティング
3. 価格データ: 取引所公開 WebSocket(無料。Phase 1 はこれのみで開始可能)
   - Phase 1.5: イベントソースの選定・契約(§7。経済指標カレンダー無料+ニュースアグリゲータ月 2 万円以下を推奨)
4. Phase 2 のみ: 国内暗号資産交換業者の口座+API キー(出金権限なし)、投入資金額と損失許容上限の決定
5. 通知先(Slack/LINE)
6. 税務(Phase 2 以降): 暗号資産の損益は雑所得。損益集計は F-6 の記録から自動生成
7. 運用体制: 週次で人間が判断ログ・成績をレビューする運用ルール、緊急停止手順の確認(取引所側での API キー無効化を含む)

## 9. 開発フェーズ

| Phase | 内容 | 完了条件 | 期間目安 |
|-------|------|----------|----------|
| 0 | デザイン確定(モックアップへのフィードバック)・要件確定 | 本書+モックの承認 | 〜1 週間 |
| 1 | ペーパートレード版+ダッシュボード(F-1〜F-4, F-6〜F-13) | **口座・資金なしで「眺められる」状態が完成**+ペーパートレード 1 ヶ月の成績・判断ログのレビュー完了 | 3〜5 週間+検証 1 ヶ月 |
| 1.5 | イベント連携+マルチエージェント化(F-15〜F-21): 要人発言・経済指標フィード、カレンダー管理、リスク管理・相談役エージェント | 高インパクトイベント/指標発表前に取引抑制が自動発動し、各エージェントの発言が思考ログに紐づくことを確認 | 2〜3 週間 |
| 2 | 実弾接続(F-5)・少額運用 | ガードレール発動テスト合格。**Phase 1 のペーパー成績レビューで優位性を確認できなければ実弾移行しない** | 2 週間+検証 1 ヶ月 |
| 3 | 戦略改善・マルチアセット(F-14) | Phase 2 実績評価後に判断 | 継続 |

**Phase 1 だけで本プロダクトの核となる価値(眺める体験)は完成する。** 実弾はその後の判断で良い。

## 10. リスク・免責事項

- LLM の判断は誤る。ペーパートレードでの成績が実弾で再現される保証はない
- 実弾移行時の元本割れリスクは発注者が負う。本システムは利益を保証しない
- ダッシュボードの見た目の良さと運用成績は無関係である(参照ポストのような成績を約束しない)

## 11. 決定事項・未決事項

### 決定済み
- **対象市場: 仮想通貨(BTC/ETH)で Phase 1 開始**(2026-07-11 発注者確定)。FX は次のアセット(F-14)
- マルチエージェント構成(トレーダー/リスク管理/相談役)の採用(§3)
- **Phase 1 完了・main マージ(2026-07-13, PR #1)**。レビュー(High 4 件・Medium 4 件)対応済み、品質ゲート全合格
- **Phase 1.5 着手(2026-07-13 発注者指示)**: 実装順は ①経済指標カレンダー(F-19)+関連指標(F-18)+リスク管理エージェント(F-20)+相談役エージェント(F-21)= 無償データで完結 → ②イベントフィード連携(F-15〜F-17)= 有償ソース契約後

### Phase 1 実装に伴う決定(2026-07-12 実装。変更希望があれば指摘ください)
- **価格データ源: bitFlyer Lightning 公開 WebSocket**(BTC_JPY / ETH_JPY。口座・API キー不要、JPY 建てでモックの表示と一致、Phase 2 の国内取引所接続と地続き)。ローソク足はコア側でティックから構築し SQLite に永続化(再起動でチャート履歴を維持)
- **ペーパートレードは現物想定のロングのみ**(ショートなし)。売りは保有分の全量決済
- 約定モデル: 実勢価格 ± スリッページ 0.05%(不利方向)+ 手数料 0.15%(bitFlyer 現物相当)。取得原価は手数料込み
- **「本日」の区切りは JST の暦日**(日次損失上限・本日損益・取引回数の集計単位)
- リスクゲート既定値: 1 取引上限 ¥50,000 / 日次損失上限 ¥30,000 / 総エクスポージャ上限 ¥200,000 / 取引頻度 30 回/日 / 損切り −2%・利確 +4%(コード側で常時監視、緊急停止中も継続)。**境界の扱い: 金額上限は「ちょうど」まで許可、日次損失・回数は「到達」で新規停止**
- トレーダーの LLM は **Claude Sonnet(現行 `claude-sonnet-5`)を既定**(§3「通常モデル」の解釈。環境変数 `IA_TRADER_MODEL` で変更可)。月次コスト上限 $50(既定)超過で判断サイクルを自動停止
- 緊急停止状態・ブローカー状態(現金・ポジション)は SQLite に永続化し、プロセス再起動後も引き継ぐ
- ダッシュボード認証(N-4): 環境変数 `IA_AUTH_TOKEN` 設定時に WS/API のトークン認証を必須化(未設定時はローカル開発用に認証なし)
- 開発・検証用モードを実装: `--feed sim`(ランダムウォーク価格。画面に「SIM データ」バッジ常時表示)/ `--llm mock`(ルールベース判断。「モック判断」バッジ表示)。実 API・実データに依存せずテスト・ブラウザ検証が可能
- モック判断には RSI(14) が揃うまで(起動後約 75 分)の**検証用ウォームアップルール**(直近安値タッチで小口打診買い・±0.2% 決済)を実装。実運用(Claude 判断)には影響しない

### Phase 1.5 ①実装に伴う決定(2026-07-14 実装。変更希望があれば指摘ください)
- **関連指標(F-18)のデータ源**: Fear & Greed = alternative.me(無料 API)、VIX・ゴールド・S&P500 先物・DXY・米10年金利 = Yahoo Finance 公開チャート API(キー不要、15〜20 分遅延で許容)。5 分ポーリング、**30 分以上古い値は判断材料・リスク評価から自動除外**(誤発動防止)。`IA_MACRO_SOURCE=sim` で検証用ランダムウォークに切替(feed=sim 時は自動で sim)
- **経済指標カレンダー(F-19)は「静的ファイル+ルール生成」で事前管理**: `core/data/economic_calendar.json`(FOMC・米CPI・日銀会合等。`IA_CALENDAR_PATH` で差し替え可)+ 米雇用統計(NFP)は毎月第 1 金曜をコードで自動生成(DST 境界月は時刻 ±1h の近似、抑制ウィンドウ幅で吸収)。同一 id は静的定義が優先(手動修正可能)。**同梱 JSON の日付のうち note に「要確認」とある項目は公式スケジュールでの確認をお願いします**
- **リスク管理エージェント(F-20)はルールベースで実装**(§3 の「軽量モデル+ルールベース」のうち、抑制の判定は全てコード。LLM による解説文の付加は Phase 1.5 ②以降で検討)。抑制モードは 2 段階: **サイズ半減**(1 取引上限を半分に)/ **新規停止**(決済・損切り監視は継続)。判定ルール: 高インパクトイベントのウィンドウ内=新規停止、中インパクト=サイズ半減、VIX ≥ 35=新規停止、VIX ≥ 25=サイズ半減、本日 3 連敗=サイズ半減、日次損失が上限の 80% 到達=サイズ半減(いずれも環境変数で調整可)。**抑制状態はリスクゲート層(コード)が保持・強制**し、発動/解除は根拠付きで思考ログ・Slack・画面バッジに出す
- **相談役エージェント(F-21)**: 毎日 JST 7 時(`IA_ADVISOR_HOUR_JST`)+起動時(直近 20 時間レビューが無い場合)に、成績・判断ログ傾向・関連指標・今後のイベントを俯瞰した日次レビューを生成し、思考ログ(所感チップ)と Slack(モーニングレポート、バックログ◎の統合)に出力。**発注経路には接続しない**(助言のみ、提案の適用は人間承認)。モデルは既定 `claude-sonnet-5`(`IA_ADVISOR_MODEL`)、トレーダーと同じ月次コスト上限カウンタを共有(日次 1 回のためコスト影響は軽微)
- 権限の序列はコードで担保: リスクゲート(緊急停止>抑制>金額/頻度上限)> リスク管理(ゲートの抑制状態を設定できるのはルール評価のみ)> トレーダー(提案のみ)。相談役は序列外

### 未決(発注者判断待ち)
1. デザインモックアップ(`docs/dashboard-mockup.html`)の最終承認
2. LLM 判断サイクルの間隔(推奨: 5 分毎から開始。短いほど「動きは楽しい」が LLM コスト増)
3. ~~Phase 1(ペーパートレード)実装への着手承認~~ → 実装済み(claude/stg)。**上記「Phase 1 実装に伴う決定」の承認**(特にリスクゲート既定値・損切り/利確ライン・トレーダーのモデル選定)
4. イベント連携(§7)のデータソース月額(〜2 万円程度)の許容 — Phase 1.5 ②(F-15〜F-17)の前提
5. 通知先の確定(Slack Incoming Webhook は実装済み・URL 設定待ち。LINE 対応は必要なら追加)
6. **「Phase 1.5 ①実装に伴う決定」の承認**(特にリスク管理エージェントの抑制ルールしきい値)と、`core/data/economic_calendar.json` の「要確認」日付の確認

## 12. 追加候補バックログ(v0.5.1)

発注者との検討用。◎=強く推奨(価値高・コスト低)、○=推奨、△=様子見。

### 追加データ候補
| 候補 | 内容・なぜ効くか | 入手方法/コスト | 推奨 |
|------|----------------|----------------|------|
| 資金調達率(Funding Rate)・建玉(OI) | デリバティブの過熱度。ロング過熱(高FR+OI急増)は反落の前兆になりやすく、短期判断に直結 | Binance/Bybit 公開 API・**無料** | ◎ Phase 1 から |
| 清算(Liquidation)データ | 大量清算は急変動の燃料。連鎖清算の検知はリスク管理エージェントの警戒材料 | Coinglass API 等・無料〜数千円 | ◎ Phase 1.5 |
| BTC 現物 ETF 資金フロー | 日次の機関資金の出入り。数日〜数週の方向性バイアスに有効 | 公開データ(日次)・無料 | ◎ Phase 1.5 |
| 取引所への BTC 流入/流出(オンチェーン) | 取引所流入増=売り圧の先行指標。中期判断の質を上げる | CryptoQuant 等・無料枠あり(本格利用は月数千〜数万円) | ○ Phase 2〜 |
| ステーブルコイン供給量 | 市場の待機資金。増加はリスク資産への流入余地 | 公開データ・無料 | ○ Phase 2〜 |
| トークンアンロック・半減期等のクリプトカレンダー | 経済指標カレンダー(F-19)のクリプト版。売り圧イベントの事前警戒 | 公開カレンダー・無料 | ○ Phase 1.5(F-19 に統合) |
| SNS センチメント(X 言及量等) | 過熱・悲観の逆張り材料。ただしノイズ多くコスト高 | LunarCrush 等・有料 | △ |

### 追加機能候補
| 候補 | 内容 | 推奨 |
|------|------|------|
| 戦略プリセット切替 | 「慎重/標準/積極」をダッシュボードから 1 クリック切替(リスクパラメータ+プロンプトのセット)。眺める楽しさと安全性の両立 | ◎ Phase 1 |
| 日次モーニングレポート | 相談役(F-21)が毎朝、地合い・昨日の成績・本日の注目イベントを Slack/LINE に配信。開かなくても状況が分かる | ◎ Phase 1.5(F-21 に統合) |
| 判断リプレイ | 過去の任意時点の判断をチャート+思考ログで再生(「なぜあそこで買った?」の振り返り UX)。F-6 の記録をそのまま活用 | ○ Phase 2 |
| 戦略 A/B 並走 | 設定の異なるトレーダー 2 体をペーパーで並走させ成績比較(例: 慎重 vs 積極)。改善ループが加速し、見た目も面白い | ○ Phase 2 |
| LLM コスト・稼働パネル | API 使用量・月額推定・エージェント別呼び出し回数を可視化(N-3 の運用面) | ○ Phase 1.5 |
| モバイル PWA+プッシュ通知 | スマホのホーム画面から起動、約定・警戒をプッシュ通知 | △ Phase 3 |
| What-if シミュレータ | 「1 ヶ月前から動かしていたら」を過去データで即試算 | △(F-22 バックテストの UI 化) |
